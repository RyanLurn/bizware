{
  "name": "@bizware/nickname",
  "type": "module",
  "private": true,
  "scripts": {
    "try": "bun test/try.ts",
    "bench:col": "bun benchmark/collision.ts",
    "bench:mem": "bun benchmark/memory.ts",
    "bench:perf": "bun benchmark/performance.ts",
    "check-types": "bunx tsc --noEmit",
    "lint": "bunx eslint --cache",
    "lint:fix": "bunx eslint --cache --fix",
    "check": "bun run check-types && bun run lint"
  },
  "devDependencies": {
    "@bizware/eslint": "workspace:*",
    "@bizware/typescript": "workspace:*",
    "@types/bun": "catalog:types",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}
